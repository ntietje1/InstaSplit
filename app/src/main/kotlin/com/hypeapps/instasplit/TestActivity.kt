package com.hypeapps.instasplit

import androidx.activity.ComponentActivity

class TestActivity : ComponentActivity() {
    // No additional implementation needed; the activity does not set its own content
}
